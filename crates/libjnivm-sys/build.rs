fn main() {
    // libjnivm-sys is a pure Rust JNI VM — no C++ compilation needed.
    // The C++ libjnivm sources (jnivm/, fake-jni/, baron/) are compiled
    // by mcpelauncher-client/build.rs as needed for JniSupport.
}
